#!/bin/bash
mkdir converted
EXT = "AVI"
FILES=( "./"*."$EXT")
for FILE in "${FILES[@]}"
do
ffmpeg -fflags +genpts -i "$FILE" -c:v copy -c:a copy ./converted/"$FILE".mkv 
done
## Convert files
#ffmpeg -i kash.DAT -c:v libx264 -crf 20 -c:a aac -b:a 128k -movflags +faststart kash.mp4
